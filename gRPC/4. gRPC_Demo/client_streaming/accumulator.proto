syntax = "proto3";
package client_streaming;
service Accumulator {
  // Receives a stream of numbers, returns their sum  
  rpc Sum (stream Number) returns (Total) {}
}
message Number {
  int32 value = 1;
}
message Total {
  int32 sum = 1;
}