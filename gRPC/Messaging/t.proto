syntax = "proto3";

package example;

service StreamService {
  rpc GetMultipleResponses (RequestMessage) returns (stream ResponseMessage);
}

message RequestMessage {
  string query = 1;
}

message ResponseMessage {
  string message = 1;
}
syntax = "proto3";

package example;

service StreamService {
  rpc GetMultipleResponses (RequestMessage) returns (stream ResponseMessage);
}

message RequestMessage {
  string query = 1;
}

message ResponseMessage {
  string message = 1;
}
